<h1> Rainbow Connection </h1>
<h3> Everybody has a favorite color.</h3>


<ul>
  {{#each model as |user|}}
    <li>{{user.name}}'s favorite color is {{user.favoritecolor}}</li>
    <li> {{user.name}}'s friends are

      {{#each user.friends as |friend|}}
        &nbsp;
        {{#link-to 'User' friend.id}}
          {{friend.name}}
        {{/link-to}}
      {{/each}}

    </li>
    {{/each}}
</ul>

{{outlet}}
